# Visual Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND LAYER                              │
│                         (Next.js 14)                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │ Predict Page    │  │ Community Bets   │  │ Dashboard        │  │
│  │                 │  │                  │  │                  │  │
│  │ ┌─────────────┐ │  │ ┌──────────────┐ │  │ ┌──────────────┐ │  │
│  │ │Create Bet   │ │  │ │Counter-Bet   │ │  │ │Bet History   │ │  │
│  │ │Modal        │ │  │ │Button        │ │  │ │              │ │  │
│  │ └─────────────┘ │  │ └──────────────┘ │  │ └──────────────┘ │  │
│  └────────┬────────┘  └────────┬─────────┘  └────────┬─────────┘  │
│           │                    │                      │            │
│           └────────────────────┼──────────────────────┘            │
│                                │                                   │
│  ┌─────────────────────────────┼──────────────────────────────┐   │
│  │              REACT HOOKS LAYER                              │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  useWallet()                                        │   │   │
│  │  │  - address, balance, connect(), refreshBalance()   │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  useBetting()                                       │   │   │
│  │  │  - createBet(), joinBet(), placeBetOnMatch()       │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────┬──────────────────────────────┘   │
│                                │                                   │
└────────────────────────────────┼───────────────────────────────────┘
                                 │
                                 ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         API LAYER                                   │
│                      (Next.js API Routes)                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  POST /api/bets/create          POST /api/bets/join                │
│  ┌─────────────────────┐        ┌─────────────────────┐            │
│  │ 1. Validate input   │        │ 1. Get original bet │            │
│  │ 2. Check balance    │        │ 2. Validate amount  │            │
│  │ 3. Call contract    │        │ 3. Call contract    │            │
│  │ 4. Store metadata   │        │ 4. Update status    │            │
│  │ 5. Return bet ID    │        │ 5. Return bet ID    │            │
│  └─────────────────────┘        └─────────────────────┘            │
│           │                              │                          │
│           └──────────────┬───────────────┘                          │
│                          │                                          │
│  GET /api/coins/balance  │  POST /api/coins/webhook                │
│  ┌─────────────────────┐ │  ┌─────────────────────┐                │
│  │ Read from contract  │ │  │ Base Pay callback   │                │
│  │ Return balance      │ │  │ Call buyCoins()     │                │
│  └─────────────────────┘ │  └─────────────────────┘                │
│                          │                                          │
└──────────────────────────┼──────────────────────────────────────────┘
                           │
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    BLOCKCHAIN LAYER                                 │
│                     (Base Network)                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │              PredictlyCoin.sol (ERC-20 + Betting)             │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │                                                               │ │
│  │  State Variables:                                             │ │
│  │  ├─ coinBalances: mapping(address => uint256)                │ │
│  │  ├─ bets: mapping(betId => Bet)                              │ │
│  │  ├─ treasuryBalance: uint256                                 │ │
│  │  └─ APP_FEE_PERCENTAGE: 5                                    │ │
│  │                                                               │ │
│  │  Public Functions:                                            │ │
│  │  ├─ buyCoins(user, amount)         → Add coins               │ │
│  │  ├─ placeBet(...)                  → Create bet, deduct coins│ │
│  │  ├─ resolveBet(betId, won, mult)   → Distribute winnings    │ │
│  │  ├─ getBalance(user)                → Query balance          │ │
│  │  ├─ getUserBets(user)               → List user's bets       │ │
│  │  └─ withdrawCoins(user, amount)    → Convert to Base        │ │
│  │                                                               │ │
│  │  Internal Functions:                                          │ │
│  │  └─ _distributeWinnings(winner, amount)                      │ │
│  │       ├─ Calculate 5% fee                                    │ │
│  │       ├─ Winner gets 95%                                     │ │
│  │       └─ Treasury gets 5%                                    │ │
│  │                                                               │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │           PredictlyBetting.sol (Match Management)             │ │
│  ├───────────────────────────────────────────────────────────────┤ │
│  │                                                               │ │
│  │  ├─ createMatch(...)                → Admin only             │ │
│  │  ├─ updateMatchStatus(...)          → Admin only             │ │
│  │  ├─ setMatchResult(...)             → Admin only             │ │
│  │  ├─ placeBetOnMatch(...)            → Calls PredictlyCoin   │ │
│  │  └─ resolveMatchBets(...)           → Batch resolution       │ │
│  │                                                               │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Create Bet Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Clicks "Create Bet"
     ↓
┌──────────────────┐
│ CreateBetModal   │
│                  │
│ ┌──────────────┐ │
│ │ Prediction   │ │  User fills form:
│ │ Position     │ │  - Prediction: "Home Win"
│ │ Amount       │ │  - Position: "For"
│ └──────────────┘ │  - Amount: 10 PDC
└────┬─────────────┘
     │
     │ 2. Calls useBetting().createBet()
     ↓
┌──────────────────┐
│ useBetting Hook  │
│                  │
│ createBet({      │
│   matchId,       │
│   amount,        │
│   prediction,    │
│   position       │
│ })               │
└────┬─────────────┘
     │
     │ 3. POST /api/bets/create
     ↓
┌──────────────────────────────┐
│ API: /api/bets/create        │
│                              │
│ 1. Validate wallet address   │
│ 2. Check user balance        │ ←─┐
│ 3. Parse amount to wei       │   │
│ 4. Call blockchain ──────────┼───┤
│ 5. Wait for confirmation     │   │
│ 6. Get bet ID from logs      │   │
│ 7. Store community metadata  │   │
│ 8. Return result             │   │
└────┬─────────────────────────┘   │
     │                              │
     │ 4. walletClient.writeContract│
     ↓                              │
┌────────────────────────────────┐ │
│ Smart Contract                 │ │
│ PredictlyCoin.placeBet()       │ │
│                                │ │
│ ┌────────────────────────────┐ │ │
│ │ 1. Check user balance      │ │ │
│ │    require(balance >= amt) │ │ │
│ │                            │ │ │
│ │ 2. Deduct coins            │ │ │
│ │    coinBalances[user] -= amt│◄┘
│ │                            │ │
│ │ 3. Create bet record       │ │
│ │    betId = betCounter++    │ │
│ │    bets[betId] = Bet(...)  │ │
│ │                            │ │
│ │ 4. Emit event              │ │
│ │    emit BetPlaced(...)     │ │
│ │                            │ │
│ │ 5. Return betId            │ │
│ └────────────────────────────┘ │
└────┬───────────────────────────┘
     │
     │ 5. Transaction confirmed
     ↓
┌──────────────────┐
│ community-bets   │
│ .json            │
│                  │
│ {                │
│   id: "bet_abc", │
│   betId: 1,      │
│   creator: 0x... │
│   amount: "10",  │
│   status: "open" │
│ }                │
└────┬─────────────┘
     │
     │ 6. Success response
     ↓
┌──────────────────┐
│  User sees       │
│  Success Modal   │
│  ✅ Bet Created! │
└──────────────────┘
```

---

## 🔁 Counter-Bet Flow

```
┌─────────┐
│  Bob    │
└────┬────┘
     │
     │ 1. Sees Alice's bet in feed
     ↓
┌──────────────────────────┐
│ Community Bets Page      │
│                          │
│ ┌──────────────────────┐ │
│ │ Alice's Bet          │ │
│ │ ├─ "Home Win" (For)  │ │
│ │ ├─ Amount: 10 PDC    │ │
│ │ └─ Status: Open      │ │
│ │                      │ │
│ │ [Bet Against This] ←─┼─┤ Bob clicks
│ └──────────────────────┘ │
└────┬─────────────────────┘
     │
     │ 2. CounterBetButton opens modal
     ↓
┌──────────────────────────┐
│ Counter-Bet Modal        │
│                          │
│ Original: Home Win (For) │
│ Your Position: Against   │
│                          │
│ Amount: [10] PDC         │
│                          │
│ [Counter-Bet]            │
└────┬─────────────────────┘
     │
     │ 3. Calls useBetting().joinBet()
     ↓
┌──────────────────┐
│ useBetting Hook  │
│                  │
│ joinBet({        │
│   communityBetId,│
│   amount,        │
│   position       │
│ })               │
└────┬─────────────┘
     │
     │ 4. POST /api/bets/join
     ↓
┌──────────────────────────────────┐
│ API: /api/bets/join              │
│                                  │
│ 1. Get original bet from storage │
│ 2. Validate amount & position    │
│ 3. Determine opposite prediction │
│    If original: "Home Win"       │
│    Then counter: "Away Win"      │
│ 4. Call blockchain ──────────────┼───┐
│ 5. Update bet status to "matched"│   │
│ 6. Add counter-bet to array      │   │
└────┬─────────────────────────────┘   │
     │                                  │
     │ 5. placeBet() on blockchain      │
     ↓                                  │
┌────────────────────────────────┐     │
│ Smart Contract                 │     │
│ PredictlyCoin.placeBet()       │     │
│                                │     │
│ Bob's bet:                     │     │
│ ├─ prediction: "Away Win"      │◄────┘
│ ├─ amount: 10 PDC              │
│ └─ betId: 2                    │
│                                │
│ (Same process as Create Bet)   │
└────┬───────────────────────────┘
     │
     │ 6. Update storage
     ↓
┌──────────────────────┐
│ community-bets.json  │
│                      │
│ {                    │
│   id: "bet_abc",     │
│   betId: 1,          │
│   creator: 0xAlice,  │
│   status: "matched", │ ← Updated
│   counterBets: [     │ ← Added
│     {                │
│       betId: 2,      │
│       bettor: 0xBob, │
│       amount: "10"   │
│     }                │
│   ]                  │
│ }                    │
└──────────────────────┘
```

---

## 💰 Bet Resolution Flow

```
Match Ends: Man Utd 2 - 1 Liverpool
Result: Home Win ✅

┌─────────────────────────┐
│  Admin                  │
│  (Contract Owner)       │
└────┬────────────────────┘
     │
     │ 1. Verifies result
     ↓
┌─────────────────────────────────┐
│ Admin Backend Script            │
│                                 │
│ For each bet in match:          │
│                                 │
│ resolveBet(                     │
│   betId: 1,                     │
│   won: true,    // Alice won    │
│   multiplier: 2  // 2x payout   │
│ )                               │
│                                 │
│ resolveBet(                     │
│   betId: 2,                     │
│   won: false,   // Bob lost     │
│   multiplier: 0                 │
│ )                               │
└────┬────────────────────────────┘
     │
     │ 2. Calls contract
     ↓
┌──────────────────────────────────────────┐
│ Smart Contract: resolveBet(1, true, 2)   │
│                                          │
│ Alice's Bet (betId: 1)                   │
│                                          │
│ 1. Mark bet as resolved                  │
│    bets[1].resolved = true               │
│    bets[1].won = true                    │
│                                          │
│ 2. Calculate winnings                    │
│    totalWinnings = 10 * 2 = 20 PDC       │
│                                          │
│ 3. Call _distributeWinnings()            │
│    ├─ fee = 20 * 0.05 = 1 PDC            │
│    ├─ net = 20 - 1 = 19 PDC              │
│    ├─ coinBalances[Alice] += 19          │
│    └─ treasuryBalance += 1               │
│                                          │
│ 4. Emit events                           │
│    emit BetResolved(1, Alice, 20, true)  │
│    emit WinningsDistributed(Alice, 19, 1)│
│                                          │
└──────────────────────────────────────────┘
     │
     ↓
┌──────────────────────────────────────────┐
│ Smart Contract: resolveBet(2, false, 0)  │
│                                          │
│ Bob's Bet (betId: 2)                     │
│                                          │
│ 1. Mark bet as resolved                  │
│    bets[2].resolved = true               │
│    bets[2].won = false                   │
│                                          │
│ 2. No winnings (lost)                    │
│    bets[2].winnings = 0                  │
│                                          │
│ 3. Emit event                            │
│    emit BetResolved(2, Bob, 0, false)    │
│                                          │
└──────────────────────────────────────────┘
     │
     ↓
┌─────────────────────────────────┐
│ Final Balances                  │
│                                 │
│ Alice:                          │
│ ├─ Before: 100 PDC              │
│ ├─ Bet: -10 PDC                 │
│ ├─ Won: +19 PDC (after 5% fee)  │
│ └─ After: 109 PDC ✅            │
│                                 │
│ Bob:                            │
│ ├─ Before: 100 PDC              │
│ ├─ Bet: -10 PDC                 │
│ ├─ Won: 0 PDC                   │
│ └─ After: 90 PDC ❌             │
│                                 │
│ Treasury:                       │
│ └─ Fee collected: +1 PDC 💰     │
│                                 │
└─────────────────────────────────┘
```

---

## 🎯 Component Interaction Map

```
┌────────────────────────────────────────────────────────────┐
│                    User Interface Layer                    │
└────────────────────────────────────────────────────────────┘

    ┌───────────────┐         ┌──────────────────┐
    │ Predict Page  │         │ Community Bets   │
    │               │         │ Page             │
    │ - Match list  │         │ - Bet feed       │
    │ - Quick bet   │         │ - Filters        │
    │ - [Create]───┐│         │ - [Counter-Bet]──┐
    └───────────────┘│         └──────────────────┘│
                     │                             │
                     ↓                             ↓
            ┌─────────────────┐         ┌──────────────────┐
            │ CreateBetModal  │         │ CounterBetButton │
            │                 │         │                  │
            │ Uses:           │         │ Uses:            │
            │ - useWallet()   │         │ - useWallet()    │
            │ - useBetting()  │         │ - useBetting()   │
            └────────┬────────┘         └────────┬─────────┘
                     │                           │
                     └───────────┬───────────────┘
                                 │
┌────────────────────────────────┼────────────────────────────┐
│               React Hooks Layer│                            │
│                                ↓                            │
│    ┌───────────────────────────────────────────────────┐   │
│    │  useWallet()                                      │   │
│    │  - Wraps useAuth() from rootProvider             │   │
│    │  - Adds balance management                       │   │
│    │  - Provides refresh functionality                │   │
│    └──────────────────┬────────────────────────────────┘   │
│                       │                                    │
│    ┌───────────────────────────────────────────────────┐   │
│    │  useBetting()                                     │   │
│    │  - createBet() → POST /api/bets/create           │   │
│    │  - joinBet() → POST /api/bets/join               │   │
│    │  - Auto-refreshes balance after bet              │   │
│    └──────────────────┬────────────────────────────────┘   │
└───────────────────────┼────────────────────────────────────┘
                        │
┌───────────────────────┼────────────────────────────────────┐
│          API Layer    ↓                                    │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │ /api/bets/create                                 │     │
│  │ - Validates user input                           │     │
│  │ - Checks balance via contract                    │     │
│  │ - Calls contract.placeBet()                      │     │
│  │ - Stores community metadata                      │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │ /api/bets/join                                   │     │
│  │ - Loads original bet                             │     │
│  │ - Determines opposite prediction                 │     │
│  │ - Calls contract.placeBet()                      │     │
│  │ - Updates bet status                             │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
└───────────────────────┬────────────────────────────────────┘
                        │
┌───────────────────────┼────────────────────────────────────┐
│      Blockchain Layer ↓                                    │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │ PredictlyCoin.placeBet()                         │     │
│  │ - Validates balance                              │     │
│  │ - Deducts coins                                  │     │
│  │ - Creates bet record                             │     │
│  │ - Emits events                                   │     │
│  │ - Returns betId                                  │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │ PredictlyCoin.resolveBet()                       │     │
│  │ - Marks bet as resolved                          │     │
│  │ - Calculates winnings                            │     │
│  │ - Distributes with 5% fee                        │     │
│  │ - Updates balances                               │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 📊 State Management

```
┌─────────────────────────────────────────────────────────────┐
│                  Application State                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Blockchain State (Source of Truth)                        │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ PredictlyCoin Contract                                │ │
│  │                                                       │ │
│  │ - coinBalances[address]: uint256                     │ │
│  │ - bets[betId]: Bet struct                            │ │
│  │ - userBets[address]: betId[]                         │ │
│  │ - treasuryBalance: uint256                           │ │
│  │                                                       │ │
│  │ Read via:                                             │ │
│  │ - publicClient.readContract()                        │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  Frontend State (React)                                    │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ useWallet()                                           │ │
│  │ ├─ address: string | null                            │ │
│  │ ├─ balance: string (cached)                          │ │
│  │ ├─ isConnected: boolean                              │ │
│  │ └─ isLoading: boolean                                │ │
│  │                                                       │ │
│  │ useBetting()                                          │ │
│  │ ├─ isPlacingBet: boolean                             │ │
│  │ └─ error: string | null                              │ │
│  │                                                       │ │
│  │ Component State                                       │ │
│  │ ├─ showModal: boolean                                │ │
│  │ ├─ amount: string                                    │ │
│  │ ├─ prediction: string                                │ │
│  │ └─ position: 'for' | 'against'                       │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  Database State (JSON/DB)                                  │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ community-bets.json                                   │ │
│  │                                                       │ │
│  │ [{                                                    │ │
│  │   id: string,              // Frontend ID            │ │
│  │   betId: number,           // Blockchain ID          │ │
│  │   creator: string,         // Wallet address         │ │
│  │   matchId: string,                                   │ │
│  │   amount: string,                                    │ │
│  │   prediction: string,                                │ │
│  │   position: 'for' | 'against',                       │ │
│  │   status: 'open' | 'matched' | 'resolved',           │ │
│  │   counterBets: CounterBet[],                         │ │
│  │   createdAt: number                                  │ │
│  │ }]                                                    │ │
│  │                                                       │ │
│  │ Used for:                                             │ │
│  │ - UI metadata                                        │ │
│  │ - Bet matching                                       │ │
│  │ - Feed display                                       │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

This comprehensive visual guide should make it much easier to understand how all the pieces fit together! 🎨📊

